plugins {
    id 'java'
}

group = 'com.insideblock'
version = '1.0.0'

repositories {
    mavenCentral()
}

dependencies {
    // Add any dependencies here
}

tasks.register('customJar', Jar) {
    from sourceSets.main.output
    archiveFileName = 'insideblock-1.0.0.jar'
}

build.dependsOn customJar
